// 주석 추가하는 법: 주석 추가: Ctrl + K, 그 다음 Ctrl + C / 주석 제거 : Ctrl + K, 그 다음 Ctrl + U

// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

/* 첫 번째 프로그램 */


//#include <stdio.h> // 헤더 파일 stdio.h를 포함시킨다. 이 파일은 표준 입출력 함수를 포함하고 있다.
//
//void main(void) // main 함수를 정의한다. 프로그램이 실행될 때, 제일 먼저 실행되는 함수는 main 함수이다. void는 '정해지지 않았다'는 의미의 예약어다.
//{
//    printf("Hello world!"); // printf 함수를 호출하여 "Hello world!" 라는 문자열을 출력한다.
//} // main 함수의 끝

// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

// 함수 정의하기


//// 헤더 파일 stdio.h를 포함시킨다. 이 파일은 표준 입출력 함수를 포함하고 있다.
//#include <stdio.h>

//// Sum이라는 함수를 정의한다. 이 함수는 두 개의 정수 인자를 받아 이를 더한 결과를 반환한다.
//int Sum(int value1, int value2) 
//{
//    int result = value1 + value2; // result라는 이름의 정수 변수를 선언하고 이를 두 인자의 합으로 초기화한다.
//    return result; // 함수의 반환 값을 result로 설정한다.
//} // Sum 함수의 끝
//
//// main 함수를 정의한다. 프로그램이 실행될 때, 제일 먼저 실행되는 함수는 main 함수이다.
//void main() 
//{
//    int s; // s라는 이름의 정수 변수를 선언한다.
//    s = Sum(2, 3); // Sum 함수를 호출하고 그 결과를 s에 저장한다. 여기서 Sum 함수의 인자로 2와 3이 주어진다.
//    printf("더하기 작업의 결과는 %d \n", s); // printf 함수를 사용하여 s의 값을 출력한다. 이 때 '%d'는 정수를 출력하는 포맷 지정자이다.
//} // main 함수의 끝

// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

// 함수 원형 선언하기


//// 헤더 파일 stdio.h를 포함시킨다. 이 파일은 표준 입출력 함수를 포함하고 있다.
//#include <stdio.h>
//
//// sum이라는 이름의 함수를 선언한다. 이 함수는 두 개의 정수 인자를 받아 이를 더한 결과를 반환한다.
//int sum(int value1, int value2);
//
//// main 함수를 정의한다. 프로그램이 실행될 때, 제일 먼저 실행되는 함수는 main 함수이다.
//void main()
//{
//    // s라는 이름의 정수 변수를 선언한다.
//    int s;
//    // sum 함수를 호출하고 그 결과를 s에 저장한다. 여기서 sum 함수의 인자로 2와 3이 주어진다.
//    s = sum(2, 3);
//} // main 함수의 끝
//
//// 이전에 선언된 sum 함수의 정의(또는 구현)이다.
//int sum(int value1, int value2)
//{
//    // result라는 이름의 정수 변수를 선언하고 이를 두 인자의 합으로 초기화한다.
//    int result = value1 + value2;
//    // 함수의 반환 값을 result로 설정한다.
//    return result;
//} // sum 함수의 끝

// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

void main()
{
	int result1, result2
}


// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ



// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ



// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
